package com.hoggen.sublimation.enums;

public enum UpLoadEnum {
    // 机构肖像图片(机构id)
    DEPARTMENTIMG,
    // 医院肖像图片(医院id)
    HOSPITALIMG,
    // 聊天图片(医生id和用户的id拼接)
    CHATIMG,
    // 医生肖像图片(医生id)
    DOCTORPORTRAIT,
    // 用户肖像图片(用户id)
    USERPORTRAIT,
    // 护士肖像图片(护士id)
    NURSEPORTRAIT,
    // 轮播图
    IMAGESHUFFLING,
    // 模块
    MODULE,
    // 复诊
    RETURNVISIT,
    /** 首诊诊断 */
    FIRST_DIAGNOSIS_TEXT,
    MULTI_SEND_MEESGAE
}
